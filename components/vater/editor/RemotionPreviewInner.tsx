"use client";

/**
 * Stub — superseded by RemotionPreview.tsx which now handles the dynamic
 * Player import directly. Kept to prevent Vercel's deployment cache from
 * serving the old version with a broken Player<T> generic.
 */
export {};
